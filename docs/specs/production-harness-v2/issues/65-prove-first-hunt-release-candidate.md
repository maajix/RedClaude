# 65 — Prove the first-hunt release candidate

**What to build:** Demonstrate from a fresh installation that RedKraken v2 can conduct a safe, complete synthetic bug-bounty hunt and leave the operator with a validated configuration and procedure for the first authorized live Program.

**Blocked by:** 64 — Run and remediate the final code review; 87 — Serve a Skill script as a recorded Tool run; 88 — Serve a fixture whose ground truth is its TLS configuration; 89 — Evaluate the agent-browser Skill set against the browser slice; 90 — Evaluate CodeGraph as the engagement graph; 91 — Rewrite the browser-evidence Skill with what the reading taught; 92 — Read a method-prefixed route template out of a source Artifact; 93 — Take the unintercepted transport measurement; 98 — Let a playbook step reach the out-of-band channel; 101 — Rewrite the playbook corpus on the capabilities that now exist; 102 — Nothing in this tree has ever created a Finding; 103 — The impact, severity, pivot and chain verbs have no caller either; 106 — Hand back the Artifact labels for the exchange the door just filed; 107 — A label minted after launch must be resolvable in the run that minted it; 111 — Close the open-text columns nine surface facts are computed from; 112 — No Identity is ever privileged, and a surface fact is built on one; 114 — The retest lane has no input and no reader; 117 — The CIDR arm of scope evaluation has no writer; 115 — Re-issue the column comment migration 018 left behind; 118 — An Identity Entity is left classified denied; 121 — The Playbook selection funnel never closes; 123 — A secret read does not name the exchange it was made for; 126 — The eval store is connected at neither end; 128 — Three Python capabilities with no caller; 130 — Check the wiring the way check_audit checks the tracker; 113 — Open redirection is routed to a class, a trigger and a Playbook that do not line up; 127 — The embedding half of dedup has no producer; 108 — A tool result is narrower than the value it was built from; 125 — A redaction miss and a report blocker cannot be recorded.; 138 — The incident response sweep has no caller; 141 — A Test specification has no producer a hunt can reach; 147 — A hunt mission never names the claim it was minted to settle; 150 — Nothing records which verbs a child reached for; 151 — The supervisor channel is built only where a tool image is named; 152 — A Test is authored and nothing performs it; 153 — The door has no address this machine can reach; 154 — A Test is authored with actions the door would refuse; 155 — An evidence edge written inside its claim is never read; 143 — An undispatchable Task refuses the pass instead of itself; 157 — The address of a subject is answered twice; 156 — A supported claim opens no work; 158 — A Task the scheduler will never call ready never ends; 164 — Fifty Playbooks, and not one has ever been selected; 142 — A suggested Task is read by nothing; 146 — The Agent credential loses its link on every token refresh; 148 — A refused claim's real reason is rolled back with it; 149 — A Tool run that errors records no reason; 161 — A chooser that ran out of budget is reported as an empty queue; 163 — A child must name a vulnerability class it is never shown; 165 — A token ceiling that sums the prefix is a turn ceiling; 104 — Let the model ask to be parked for a human; 159 — Recon writes no Host and no edge between a name and what serves it; 131 — An egress run is opened under no Identity at all; 133 — A privileged slot silently disarms a Playbook; 136 — An HTTP answer names neither its scope class nor its Identity; 185 — Draw the campaign as a graph; 186 — The source analysis chain has no way to start; 189 — No installation names the image the tools live in; 191 — Work the engagement in both states; 193 — A root mapped as nobody is not mapped; 192 — A Test is spent in a state its claim was never made in; 194 — A capability that lapsed mid-connect still names its run; 197 — The suite rotates a live hunt's password; 198 — The one offline tool handed a carrier it cannot read; 196 — The queue has never been ranked; 199 — No rung of the ladder can finish a campaign; 202 — A teardown that raises leaves its fixtures standing; 203 — A lane with no floor is still a lane; 201 — The database suite has been failing unseen; 204 — Two states of one claim halve every chain unlock; 205 — Fixture debris holds a clamped lane shut; 206 — One open question stops the whole campaign; 207 — The Rules of Engagement say logged in, and the door asks anyway; 208 — An empty Slate does not say which wall the pass hit; 209 — Nobody sweeps the questions, and the control surface stops the campaign; 212 — A path parameter walks out of an authorised prefix; 174 — Chromium runs with its own sandbox off and no profile to turn it on; 145 — An observation kind that takes only a tool run is offered a receipt; 215 — A dead correlator is graded against the transaction's clock; 225 — The door announces no version to anything; 105 — Two request contracts name a queue nothing serves; 213 — Two door classes fail only when they share a database; 218 — A corpus rewrite retires its stranded Tasks under the wrong reason; 219 — One hunt per machine is the Agent network's rule, not the scheduler's; 220 — A migration that changes what a writer must write does not reach the writer; 221 — Severity waits on a validation nothing in this runtime can ask for; 222 — rk finding validate needs a run that only exists while it cannot run; 223 — A window nothing closes is not a window; 227 — The scheduler cannot tell a critical claim from a header; 228 — One broken notifier halts the whole harness; 229 — The only notifier a Finding has is not in this repository; 230 — A retest reopens a claim nothing can settle; 231 — Payment processes need methods, not leads; 216 — A Playbook halt has no question code to park under; 166 — A Playbook demands an Observation kind no verb can write; 234 — A declared halt parks or reports, and two files disagree; 235 — The corpus gate is red: a shipped Playbook has no ledger record; 236 — The documented database test command runs nothing; 134 — An address range has no breadth floor; 233 — A probe-only Playbook bar asks for two kinds its own trigger refuses; 226 — The kill chain analysis has never held a single row
**Status:** ready-for-agent

- [ ] A fresh operator configures and starts a realistic synthetic web/API Program using only supported documentation, CLI/UI and secret handling.
- [ ] The campaign performs scoped recon, selects production Skills and Playbooks, records a meaningful negative result and discovers at least one fixture vulnerability.
- [ ] The vulnerability progresses through Observation, Evidence, Hypothesis, structured Test, replay, blind validation and deterministic Finding report without manual database intervention.
- [ ] A demonstrated pivot is evaluated in a sound kill chain, while an intentionally missing or invalid pivot remains visibly unreportable.
- [ ] Forced pause, process restart and resume preserve truth, budgets, Leases and bounded context and do not repeat settled work incorrectly.
- [ ] The final evidence bundle verifies independently and contains no synthetic credentials or unredacted wire secrets.
- [ ] The release includes an operator checklist and fillable Program configuration for the first authorized live hunt, with explicit scope, RoE, identity, budget, callback and stop prerequisites.
