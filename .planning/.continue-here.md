---
context: default
phase: startup-assertion
task: 2
total_tasks: 8
status: in_progress
last_updated: 2026-08-08T23:51:56+02:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: exact test execution — `run_all.sh` previously ignored a failed `psql` process and reported green before Group M ran; preserve the exit-code check and the exact 86-result assertion.
- [ ] CONSTRAINT: result rows outside rollback — six Group M probes inserted their result inside the subtransaction they deliberately rolled back; keep result insertion outside that rollback boundary.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Count prose as test proof | The old runner printed `56 checks, 0 failing` after aborting before Group M. | blocking | Require successful `psql` exit and exactly 86 persisted results. |
| Composite insert into generated columns | `INSERT ... SELECT receipts%ROWTYPE.*` failed because two transport columns are `GENERATED ALWAYS`. | advisory | Keep the writer's explicit non-generated column list. |
| xmin probe inside exception subtransaction | M06 produced false `row_last_write_unaccounted` findings from its own subxid. | advisory | Run that disposable probe without an exception block and purge its program afterward. |

<current_state>
Ticket 02 is implemented but not committed on branch `implementation/receipt-capability` in `/home/majix/redKrakenV2/.worktrees/startup-launch`. The final full schema run finished green immediately after the pause request; no further feature work was done afterward.
</current_state>

<completed_work>

- Added `20260808T213113Z__startup_receipt_capabilities.sql`: database-owned gate stamps, random 256-bit capability, SHA-256-only state, five-minute expiry, active program/run/task resolution, derived allowed-receipt writer, and capability revocation on terminal state.
- Kept the legacy receipt path as ticket 02 requires; ticket 06 remains the contraction point.
- Changed `agent_child.py` to call `authorize_tool_run`, never write decision fields directly, and keep plaintext capabilities out of serialised `STATE`.
- Added K01–K04 adversarial SQL tests and one Python no-leak contract test.
- Repaired previously hidden schema-test false greens in Groups B/M and made `run_all.sh` fail on SQL abort or a missing result.
- Verified full schema lifecycle: 38 migrations, 113 managed tables, 19 standing checks, 86 checks, dump/restore, exit 0.
- Verified `python -m unittest -v test_startup_launch.py`: 11 tests, all green.
</completed_work>

<remaining_work>

- Run the non-live walking-skeleton validation. `skeleton.py` still has several direct inserts of `risk_class`/`decision`; route real runtime cases through `authorize_tool_run` or make clearly synthetic owner fixtures explicit.
- Check the P6 agent request that currently passes `task_id=None` while its parent run owns a task; the new resolver intentionally requires exact run/task binding.
- Review the final SQL security boundary and complete diff-only secret scanning/Gitleaks.
- Run final focused/full checks after any walking-skeleton fixes.
- Commit Ticket 02 atomically.
- Update `/home/majix/redKrakenV2/.scratch/startup-assertion/issues/02-expand-capability-backed-receipts.md`, `spec.md`, and the implementation map to resolved/current frontier.
- Continue with Ticket 04 or Ticket 05 after Ticket 02 is closed.
</remaining_work>

<decisions_made>

- The database, not the hook, owns `risk_class`, `decision`, `decision_reason`, capability digest and expiry.
- PostgreSQL `pgcrypto` supplies `gen_random_bytes(32)` and `digest`; no new application dependency or token service.
- `write_allowed_receipt` overwrites caller-supplied program, tool-run, lane, decision and scope-version fields with resolved values.
- Parking reuses the existing transaction behind a security-definer wrapper after the same authorization check; its body was not duplicated.
- Plaintext capability stays in a private process map until Ticket 04 adds the proxy carrier.
</decisions_made>

<blockers>

- None. The session is paused only at the user's request.
</blockers>

## Required Reading (in order)

1. `/home/majix/redKrakenV2/.scratch/startup-assertion/issues/02-expand-capability-backed-receipts.md` — exact Ticket 02 acceptance boundary.
2. `/home/majix/redKrakenV2/.scratch/startup-assertion/spec.md` — especially “Adjacent architecture decision: the receipt is the ticket-57 fence”.
3. `prototype/schema/migrations/20260808T213113Z__startup_receipt_capabilities.sql` — implementation under review.
4. `prototype/schema/tests/capability_receipts.sql` — positive and negative contract.
5. `prototype/walking-skeleton/agent_child.py` and `skeleton.py` — runtime path and remaining legacy demo writers.

## Critical Anti-Patterns (do NOT repeat these)

- Do not infer green from a result count unless the SQL process itself exited successfully and all 86 IDs persisted.
- Do not put a probe result inside the same subtransaction deliberately raised to roll the fixture back.
- Do not serialize, log, persist or include the plaintext capability in an error message.
- Do not remove the legacy receipt path in Ticket 02; that belongs to Ticket 06.

## Infrastructure State

- Worktree: `/home/majix/redKrakenV2/.worktrees/startup-launch`
- Branch/implementation base: `implementation/receipt-capability` at `61ac4e1`; current HEAD is the WIP handoff commit
- Disposable test container: `rk2-ticket02-red`, stopped/exited
- Background processes: none
- Root `.scratch` ticket/spec files remain uncommitted and still say Ticket 02 is ready; update only after implementation validation and commit.

<context>
The database path and focused runtime seam are working. The important remaining uncertainty is integration fallout in the older walking-skeleton scenarios, which still manufacture decided tool runs directly. Start there, keep the Ticket 02 expansion boundary, then perform the security/diff checks and commit.
</context>

<next_action>
Start with: acknowledge both blocking constraints above, then inspect `prototype/walking-skeleton/run_all.sh` and run its non-live path to identify the smallest required updates to remaining direct decision writers.
</next_action>
