# redKrakenV2

## Agent skills

### Issue tracker

Issues live as local markdown files under `docs/specs/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, each recorded as a `Status:` line in the issue file. See `docs/agents/triage-labels.md`.

### Testing

Which tests to run for a change, and when the full suite is worth it. See `docs/agents/testing.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
